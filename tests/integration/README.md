# Integration Tests

Place world/system interaction tests here.

Examples:

- System ordering and deterministic event processing
- Pause/resume invariants across world + resources
- Rendering intent generation from simulation state
